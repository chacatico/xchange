import { ExchangeRatesComponent } from './exchange-rates.component';

describe( 'Comprobar cotización', () => {

  it( 'Calcular cotización', () => {
    const mensaje = 'Hola Mundo';
    console.log(mensaje);

    expect(typeof mensaje).toBe('string');
  });

});
